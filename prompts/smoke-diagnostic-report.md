你正在为 VOHU 团队撰写一份中文内部 Smoke 测试报告，而不是对外宣传稿。

只读取 `{evidence_dir}` 下冻结的证据文件。不要联网、调用模型、修改证据、重算分数或补造缺失值。
报告必须包含：标题、结论摘要、方法、三次 trial 汇总结果、质量/成本/稳定性观察、发布判断、下一步。

每个包含数字的句子都必须在句末添加精确证据标记：`<!-- evidence:path.to.value -->`。
如果 `publishable` 为 false，必须明确写“当前结果不可对外宣传”，并说明系统失败会污染固定分母。
不要声称 VOHU 领先、超过或击败任何外部模型，因为本证据没有同协议外部对照。

只返回 Markdown。
